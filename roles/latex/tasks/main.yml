---
- name: "DEV_LATEX_MAIN - Resolve users"
  ansible.builtin.include_tasks: resolve.yml

- name: "DEV_LATEX_MAIN - Install LaTeX per user"
  ansible.builtin.include_tasks: install.yml
  loop: "{{ latex_users_present }}"
  loop_control:
    label: "{{ item.name }}"
  vars:
    user: "{{ item }}"
